package ru.trendtech.utils;

/**
 * Created by petr on 26.08.14.
 */

import ru.trendtech.domain.Location;

import java.util.*;


public class SortLocationUtil{
    //List<Location> locations = new ArrayList<>();
    //final Location myLocation = null;

//    public static void startSorting(List<LongLatUtil.LocationUtil> locations) {
//
//        //LOGGER.info("location = ");
//
//        Collections.sort(locations, new Comparator<LongLatUtil.LocationUtil>() {
//            @Override
//            public int compare(LongLatUtil.LocationUtil loc1, LongLatUtil.LocationUtil loc2) {
//
//                LOGGER.info("loc1.getDistanceFromDriver() = "+loc1.getDistanceFromDriver()+" address = "+loc1.getAddress());
//                LOGGER.info("loc2.getDistanceFromDriver() = "+loc2.getDistanceFromDriver()+" address = "+loc2.getAddress());
//
//    if(loc1.getDistanceFromDriver() <= loc2.getDistanceFromDriver()){
//        LOGGER.info("loc1.getDistanceFromDriver() <= loc2.getDistanceFromDriver()");
//        return 1;
//    }else if(loc1.getDistanceFromDriver() > loc2.getDistanceFromDriver()){
//        return 0;
//    }else return -1;
//            }
//                //return distance1 < distance2 ? c1 : distance1 == distance2 ? c2 : c2;
//                //return new Double(distance1).compareTo(new Double(distance2));
//
//        });
//
//



/*
        sort(locations, new ToComparable<Location, Double>() {
            @Override
            public Double toComparable(Location location) {

                double distance = GeoUtils.distance(
                        location.getLatitude(), location.getLongitude(),
                        myLocation.getLatitude(), myLocation.getLongitude());

                LOGGER.info("distance = "+distance);
                return 0.0;// distance;//Location.distance(location, myLocation);
            }
        });
*/


        //for (Location location : locations)
           // LOGGER.info("result: "+location.getAddress());
      //  }


}